# Frontend (Phase 2)

React PWA foundation. The browser never talks to MongoDB.

```bash
cd frontend
npm install
npm run dev
```

```bash
npm run build
npm run preview
npm test
```

`npm test` checks that Dexie/IndexedDB initializes (via `fake-indexeddb` in Node).
