services:
  - type: web
    name: smriti-backend
    env: python
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: MONGODB_URI
        value: mongodb+srv://suyashaher99_db_user:wypyJh9E3caCMtj8@cluster0.kbzdrbx.mongodb.net/eldercare_ai?appName=Cluster0
      - key: CORS_ORIGINS
        sync: false
      - key: JWT_SECRET_KEY
        generateValue: true
